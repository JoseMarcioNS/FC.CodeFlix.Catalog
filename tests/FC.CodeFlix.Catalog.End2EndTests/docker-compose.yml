version: '3.4'

services:
  fc.codeflix.catalog.db:
    container_name: catalogdb_end2end
    hostname: catalogdb_end2end
    image: mysql
    ports:
      - 33060:3306
    environment:
      MYSQL_ROOT_PASSWORD: teste@123